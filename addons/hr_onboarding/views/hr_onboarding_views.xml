<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="hr_onboarding_view_form" model="ir.ui.view">
        <field name="name">hr.onboarding.form</field>
        <field name="model">hr.onboarding</field>
        <field name="arch" type="xml">
            <form string="Employee Onboarding">
                <header>
                    <button name="action_generate_tasks" type="object" string="Generate Tasks"
                        class="btn-primary" invisible="task_ids"/>
                    <button name="action_mark_done" type="object" string="Mark Completed"
                        class="btn-primary" invisible="state != 'in_progress'"/>
                    <button name="action_cancel" type="object" string="Cancel"
                        invisible="state != 'in_progress'"/>
                    <field name="state" widget="statusbar"/>
                </header>
                <sheet>
                    <div class="oe_title">
                        <h1><field name="name"/></h1>
                    </div>
                    <group>
                        <group>
                            <field name="employee_id"/>
                            <field name="template_id"/>
                        </group>
                        <group>
                            <field name="start_date"/>
                            <field name="progress" widget="progressbar"/>
                        </group>
                    </group>
                    <separator string="Tasks"/>
                    <field name="task_ids">
                        <list editable="bottom"
                            decoration-success="state == 'done'"
                            decoration-muted="state == 'skipped'">
                            <field name="sequence" widget="handle"/>
                            <field name="name"/>
                            <field name="category" widget="badge"/>
                            <field name="responsible_user_id"/>
                            <field name="deadline"/>
                            <field name="state" widget="badge"
                                decoration-info="state == 'todo'"
                                decoration-warning="state == 'in_progress'"
                                decoration-success="state == 'done'"
                            />
                            <field name="completion_date"/>
                            <button name="action_done" type="object" string="Done"
                                icon="fa-check" invisible="state in ('done', 'skipped')"/>
                            <button name="action_skip" type="object" string="Skip"
                                icon="fa-forward" invisible="state in ('done', 'skipped')"/>
                        </list>
                    </field>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

    <record id="hr_onboarding_view_list" model="ir.ui.view">
        <field name="name">hr.onboarding.list</field>
        <field name="model">hr.onboarding</field>
        <field name="arch" type="xml">
            <list string="Onboardings"
                decoration-success="state == 'done'"
                decoration-muted="state == 'cancelled'">
                <field name="employee_id"/>
                <field name="template_id"/>
                <field name="start_date"/>
                <field name="progress" widget="progressbar"/>
                <field name="state" widget="badge"
                    decoration-info="state == 'in_progress'"
                    decoration-success="state == 'done'"
                />
            </list>
        </field>
    </record>

    <record id="hr_onboarding_action" model="ir.actions.act_window">
        <field name="name">Onboardings</field>
        <field name="res_model">hr.onboarding</field>
        <field name="view_mode">list,form</field>
    </record>
</odoo>
