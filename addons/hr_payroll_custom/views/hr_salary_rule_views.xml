<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Salary Rules Action -->
    <record id="action_hr_salary_rule" model="ir.actions.act_window">
        <field name="name">Salary Rules</field>
        <field name="res_model">hr.salary.rule</field>
        <field name="view_mode">list,form</field>
    </record>

    <!-- Salary Structures Action -->
    <record id="action_hr_salary_structure" model="ir.actions.act_window">
        <field name="name">Salary Structures</field>
        <field name="res_model">hr.salary.structure</field>
        <field name="view_mode">list,form</field>
    </record>

    <!-- Menu Items -->
    <menuitem id="menu_hr_salary_structure" name="Salary Structures" parent="menu_hr_payroll_config" action="action_hr_salary_structure" sequence="10"/>
    <menuitem id="menu_hr_salary_rules" name="Salary Rules" parent="menu_hr_payroll_config" action="action_hr_salary_rule" sequence="20"/>
</odoo>
