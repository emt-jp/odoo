<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="0">

        <!-- Payroll User Group -->
        <record id="group_hr_payroll_user" model="res.groups">
            <field name="name">Payroll Officer</field>
            <field name="comment">Payroll Officers can manage payslips and process payroll</field>
            <field name="implied_ids" eval="[(4, ref('hr.group_hr_user'))]"/>
        </record>

        <!-- Payroll Manager Group -->
        <record id="group_hr_payroll_manager" model="res.groups">
            <field name="name">Payroll Manager</field>
            <field name="comment">Payroll Managers have full access to payroll features</field>
            <field name="implied_ids" eval="[(4, ref('group_hr_payroll_user')), (4, ref('hr.group_hr_manager'))]"/>
        </record>

        <!-- Record Rules -->
        <record id="payslip_user_rule" model="ir.rule">
            <field name="name">Payslip: employees: read own payslips</field>
            <field name="model_id" ref="model_hr_payslip"/>
            <field name="domain_force">[('employee_id.user_id', '=', user.id)]</field>
            <field name="groups" eval="[(4, ref('base.group_user'))]"/>
            <field name="perm_read" eval="True"/>
            <field name="perm_write" eval="False"/>
            <field name="perm_create" eval="False"/>
            <field name="perm_unlink" eval="False"/>
        </record>

        <record id="payslip_manager_rule" model="ir.rule">
            <field name="name">Payslip: payroll officers: manage all payslips</field>
            <field name="model_id" ref="model_hr_payslip"/>
            <field name="domain_force">[(1, '=', 1)]</field>
            <field name="groups" eval="[(4, ref('group_hr_payroll_user'))]"/>
            <field name="perm_read" eval="True"/>
            <field name="perm_write" eval="True"/>
            <field name="perm_create" eval="True"/>
            <field name="perm_unlink" eval="True"/>
        </record>

    </data>
</odoo>
