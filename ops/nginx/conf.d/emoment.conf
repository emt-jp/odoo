upstream odoo {
    server odoo:8069;
}

upstream odoo_chat {
    server odoo:8072;
}

# HTTP - redirect to HTTPS (after SSL is set up)
server {
    listen 80;
    server_name emoment.jp www.emoment.jp;

    # Let's Encrypt challenge
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    # Redirect root to /rent
    location = / {
        return 301 /rent;
    }

    # Proxy to Odoo (before SSL is set up)
    location / {
        proxy_pass http://odoo;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 720s;
        proxy_connect_timeout 720s;
        proxy_send_timeout 720s;
        client_max_body_size 100M;
    }

    # Websocket for live chat
    location /websocket {
        proxy_pass http://odoo_chat;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    # Static files caching
    location ~* /web/static/ {
        proxy_pass http://odoo;
        proxy_cache_valid 200 90m;
        proxy_buffering on;
        expires 7d;
        add_header Cache-Control "public, no-transform";
    }
}

# HTTPS (uncomment after running certbot)
# server {
#     listen 443 ssl http2;
#     server_name emoment.jp www.emoment.jp;
#
#     ssl_certificate /etc/letsencrypt/live/emoment.jp/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/emoment.jp/privkey.pem;
#     ssl_session_timeout 1d;
#     ssl_session_cache shared:SSL:50m;
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256;
#     ssl_prefer_server_ciphers off;
#
#     # Redirect root to /rent
#     location = / {
#         return 301 /rent;
#     }
#
#     location / {
#         proxy_pass http://odoo;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto https;
#         proxy_read_timeout 720s;
#         proxy_connect_timeout 720s;
#         proxy_send_timeout 720s;
#         client_max_body_size 100M;
#     }
#
#     location /websocket {
#         proxy_pass http://odoo_chat;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection "upgrade";
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#     }
#
#     location ~* /web/static/ {
#         proxy_pass http://odoo;
#         proxy_cache_valid 200 90m;
#         proxy_buffering on;
#         expires 7d;
#         add_header Cache-Control "public, no-transform";
#     }
# }
