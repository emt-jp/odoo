# Odoo Security Monitor Configuration

# Email Settings
EMAIL_ENABLED="true"
EMAIL_TO="hr@emoment.jp"
EMAIL_FROM="prasanta.sahoo@emoment.in"

# SMTP Settings (Brevo)
SMTP_SERVER="smtp-relay.brevo.com"
SMTP_PORT="587"
SMTP_TLS="true"
SMTP_USER="prasanta.sahoo@emoment.in"
SMTP_PASS="qDPMBSLmdYIvxgJh"

# Thresholds
PG_FAILED_LOGIN_THRESHOLD="5"   # Alert if more than N failed logins in 10 minutes
CPU_THRESHOLD="200"             # Alert if container CPU exceeds N%
MAX_PG_PROCESSES="30"           # Alert if PostgreSQL has more than N processes
DISK_THRESHOLD="85"             # Alert if disk usage exceeds N%

# Hostname for alerts
HOSTNAME="$(hostname)"
